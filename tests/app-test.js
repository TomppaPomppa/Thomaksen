import { describe, test, expect, it } from 'vitest';
const Todo = require('../public/app.js');

describe('Todo testaus', function () {
  const todo = Todo;

  it('Tarkistetaan', function () {});

  it('Tarkistetaan', function () {});
});
